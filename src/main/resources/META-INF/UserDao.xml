<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserDao">

    <resultMap id="result" type="com.example.demo.entity.UserEntity">

        <result property="uid" column="uid"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>

    </resultMap>

    <insert id="insert" parameterType="com.example.demo.entity.UserEntity">
        INSERT INTO users(uid, username, email, password)
        VALUES (#{uid}, #{username}, #{email},#{password});
    </insert>


    <select id="getByEmail" resultMap="result">
        SELECT * FROM users WHERE email=#{email};
    </select>




</mapper>